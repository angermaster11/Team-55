diff --git a/test_app.py b/test_app.py
index edc9b7a..6f7d679 100644
--- a/test_app.py
+++ b/test_app.py
@@ -1,9 +1,11 @@
 from app import add, divide
 
-def test_add():
-    assert add(2,3) == 6
+def test_add():
+    assert add(2, 3) == 5
 
 def test_divide():
-    assert divide(10,2) == 5
+    assert divide(10, 2) == 5
 
-def test_divide_by_zero():
-    divide(5,0)
-
-def test_add_string():
-    assert add("a ", "b") == "ab"
-
-def test_divide_by_zero():
-    divide(5,1)
-
-
-def test_divide_by_zero():
-    divide(5,0)
+def test_add_string():
+    assert add("a", "b") == "ab"
+
+def test_divide_by_zero():
+    divide(5, 0)
diff --git a/app.py b/app.py
index 2d3a7a1..e51814c 100644
--- a/app.py
+++ b/app.py
@@ -1,7 +1,13 @@
 import numpy as np
 
-def add(a, b):
-    return np.add(a, b)
+def add(a, b):
+    # For strings, fallback to concatenation using +
+    if isinstance(a, str) and isinstance(b, str):
+        return a + b
+    try:
+        return np.add(a, b)
+    except TypeError:
+        # fallback to Python addition for other types
+        return a + b
 
 def divide(a, b):
-    return np.divide(a, b)
+    return np.divide(a, b)
+