export interface Feature {
  icon: string;
  title: string;
  desc: string;
  color: string;
}

export interface Step {
  num: string;
  title: string;
  desc: string;
}

export interface FAQ {
  q: string;
  a: string;
}

export interface NavLink {
  label: string;
  href: string;
}
